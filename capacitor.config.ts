import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appflow-ng-deploy-api',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
